#include <stdio.h>

int main() {
    // Write C code here
    int a,b;
    scanf("%d%d",&a,&b);
    if(b==0){
    printf("b value cannot be Zero\n");
    b=1;
    }
    int r=a/b;
    printf("%d",r);

    return 0;
}
OUTPUT:
12 0
b value cannot be Zero
12
