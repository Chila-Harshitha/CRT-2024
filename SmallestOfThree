#include <stdio.h>
#include<math.h>

int main() {
    // Write C code here
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    if((a<b)&&(a<c)){
    printf("%d is small",a);
    }
    else
    if(b<c){
        printf("%d is small",b);
    }
    else
    printf("%d is small",c);

    return 0;
}
